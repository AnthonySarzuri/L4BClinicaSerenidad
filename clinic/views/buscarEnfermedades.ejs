<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buscar Enfermedades</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-6">
    <div class="container mx-auto">
        <h1 class="text-2xl font-bold mb-4">Buscar Enfermedades</h1>
        <form action="/enfermedades/buscar" method="get" class="mb-6">
            <input type="text" name="termino" placeholder="Ingrese nombre o descripción" class="border p-2 w-full" value="<%= termino || '' %>">
            <button type="submit" class="bg-blue-500 text-white px-4 py-2 mt-2 rounded">Buscar</button>
        </form>
        <% if (resultados && resultados.length > 0) { %>
            <table class="min-w-full bg-white">
                <thead>
                    <tr>
                        <th class="py-2 px-4 border-b">ID</th>
                        <th class="py-2 px-4 border-b">Nombre</th>
                        <th class="py-2 px-4 border-b">Descripción</th>
                        <th class="py-2 px-4 border-b">Estado</th>
                    </tr>
                </thead>
                <tbody>
                    <% resultados.forEach(enfermedad => { %>
                        <tr>
                            <td class="py-2 px-4 border-b"><%= enfermedad.enfermedad_id %></td>
                            <td class="py-2 px-4 border-b"><%= enfermedad.nombre %></td>
                            <td class="py-2 px-4 border-b"><%= enfermedad.descripcion %></td>
                            <td class="py-2 px-4 border-b"><%= enfermedad.estado %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p class="text-gray-600">No se encontraron resultados.</p>
        <% } %>
    </div>
</body>
</html>
